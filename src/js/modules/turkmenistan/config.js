/**
 * Единая точка настройки 3D-карты.
 * Здесь меняются пропорции, свет, цвета рельефа и поведение анимации.
 * Никаких магических чисел в остальном коде быть не должно.
 */
export const CONFIG = {
  /**
   * Общий префикс к файлам секции. Все пути ниже и в data/facilities.js
   * считаются от него. Если ресурсы лежат не в корне сайта, достаточно
   * поменять эту строку: например '/assets/turkmenistan/'.
   */
  assetsBase: '/',

  /** Границы: любой GeoJSON с Polygon/MultiPolygon и properties.id. */
  geojsonUrl: 'data/welayats.geo.json',

  /** Подготовка контуров. Все значения — в единицах сцены (world.width = 20). */
  geometry: {
    /** Упрощение границ: 0 — как в файле, 0.015 ≈ 1.7 км, 0.05 — очень грубо. */
    simplifyTolerance: 0.022,
    /** Отдельные острова меньше этой площади не строятся. */
    minIslandArea: 0.05,
    /** Дырки (анклавы) меньше этой площади заливаются. Ашхабад ≈ 0.06. */
    minHoleArea: 0.5,
    /** На сколько отступать от границы, проверяя, есть ли за ней сосед. */
    borderProbe: 0.045,
  },

  world: {
    /** Ширина страны в условных единицах сцены. Всё остальное считается от неё. */
    width: 20,
    /** Толщина «плиты» под рельефом (нижняя стенка). */
    thickness: 0.5,
  },

  /**
   * Реальный рельеф. Источник — SRTM30_PLUS (~900 м на точку), упакованный
   * в 12-битный PNG. Если файл не загрузится, секция сама откатится на
   * процедурную генерацию из блока terrain.
   */
  dem: {
    enabled: true,
    url: 'data/turkmenistan-height.png',
    /** Границы файла: [запад, юг, восток, север]. */
    bbox: [52.0, 34.8, 67.2, 43.1],
    /** Высота, соответствующая максимуму упаковки. */
    maxMeters: 3200,
    /** Единиц сцены на 1000 м высоты. Больше — горы выше. */
    verticalScale: 0.55,
    /**
     * Сжатие высот: показатель < 1 поднимает равнину и придавливает горы.
     * Именно это даёт «плакатный» рельеф вместо географически честного, но
     * почти плоского: в пустыне видно эрозию, а хребты не лезут в небо.
     */
    curve: 0.72,
    /** Во сколько раз загрубить сетку под смещение вершин. */
    coarseStep: 10,
    /** Радиус (в пикселях) для отделения мелкого рельефа под карту нормалей. */
    detailRadius: 8,
    /** Сила мелкого рельефа в освещении. */
    normalStrength: 1.8,
    /**
     * Знаки карты нормалей. Если мелкий рельеф выглядит вывернутым наизнанку
     * (гряды читаются как канавы), поменяйте знак второго числа.
     */
    normalScale: [1, 1],
    /**
     * Высота в метрах, на которой высотная палитра доходит до самого светлого
     * цвета. Страна почти вся лежит ниже 300 м, поэтому нормировать по вершине
     * Айрыбабы нельзя — пустыня стала бы одноцветной.
     */
    paletteMeters: 2200,
  },

  terrain: {
    /** Максимальная длина ребра после тесселяции: меньше — детальнее и тяжелее. */
    maxEdgeLength: 0.18,
    tessellationIterations: 14,
    /** Ширина зоны спада высоты у государственной границы (эрозия по краям). */
    edgeFalloff: 0.55,
    /** Базовый шум пустыни. */
    noise: { frequency: 0.9, amplitude: 0.5, octaves: 5, lacunarity: 2.07, gain: 0.52 },
    /** Постоянная «подушка» под шумом, чтобы плита не проваливалась. */
    baseHeight: 0.12,
    /** Мелкая «песчаная» детализация поверх основного шума. */
    detail: { frequency: 3.4, amplitude: 0.1 },
    /**
     * Гребневой шум: даёт острые гряды барханов вместо круглых холмов.
     * sharpness > 1 — гребни уже и резче.
     */
    ridged: { frequency: 1.45, amplitude: 0.38, sharpness: 2.1, octaves: 4 },
    /** Затемнение вдоль границ велаятов: width — в единицах сцены. */
    borderShade: { width: 0.12, strength: 0.46 },
    /**
     * Горные хребты и речные долины (отрицательная amplitude).
     * points — [lon, lat], width — радиус влияния в единицах сцены.
     */
    features: [
      { name: 'Kopet Dag', points: [[57.5, 38.35], [58.5, 38.0], [59.5, 37.5],
        [60.4, 37.05]], amplitude: 1.05, width: 0.9 },
      { name: 'Balkan', points: [[54.3, 39.5], [55.4, 39.6], [56.1, 39.5]], amplitude: 0.62, width: 0.7 },
      { name: 'Köýtendag', points: [[66.4, 38.4], [66.2, 37.9]], amplitude: 0.8, width: 0.5 },
      { name: 'Krasnovodsk plateau', points: [[53.4, 40.3], [54.6, 40.6]], amplitude: 0.45, width: 0.65 },
      { name: 'Amyderýa', points: [[66.2, 37.6], [64.6, 38.3], [63.2, 39.0], [61.8, 39.9], [60.4, 41.0],
        [59.4, 42.0]], amplitude: -0.42, width: 0.38 },
      { name: 'Murgap', points: [[62.4, 35.6], [62.1, 36.6], [61.9, 37.6]], amplitude: -0.3, width: 0.3 },
    ],
    /** Палитра рельефа снизу вверх: [позиция 0..1, цвет]. */
    palette: [
      [0.0, '#9c7a55'],
      [0.32, '#af8a61'],
      [0.62, '#bf9c72'],
      [0.85, '#cfb089'],
      [1.0, '#dfc7a4'],
    ],
    /** Гамма высотной палитры: <1 делает равнину светлее. */
    paletteGamma: 0.7,
  },

  markers: {
    /** Общий размер 3D-иконок в единицах сцены (ширина страны = world.width). */
    facilityScale: 0.62,
    /** Насколько поднять модель над рельефом, чтобы не утонула в склоне. */
    facilityLift: 0.01,
    /** Вписывать загруженные .glb в единичную высоту. */
    normalizeModels: true,
    /** Цветные фишки категорий работ рядом с объектом. */
    dots: {
      /** false — фишки не строятся вовсе. */
      enabled: true,
      radius: 0.11,
      height: 0.05,
      gap: 0.27,
      /** Смещение ряда фишек относительно центра модели: [x, y, z]. */
      offset: [0, 0.025, 0.52],
      emissive: 0.6,
    },
  },

  camera: {
    fov: 34,
    /** Позиция относительно центра карты (единицы сцены). */
    position: [0.8, 18.4, 14.7],
    target: [0, 0.4, 0.6],
    /** Микро-параллакс от курсора. 0 — камера неподвижна. */
    parallax: 0,

    /**
     * Приближение колесом мыши. Диапазон намеренно узкий: карта — часть
     * страницы, а не полноценный вьюер. На границах диапазона колесо снова
     * прокручивает страницу, поэтому скролл не «залипает» на секции.
     */
    zoom: {
      enabled: true,
      min: 1,
      max: 1.80,
      /** Шаг за один щелчок колеса. */
      step: 0.09,
      /** Плавность подъезда, как у подъёма велаятов. */
      damping: 0.18,
      /**
       * Насколько далеко от центра можно уехать. Предел растёт вместе с зумом
       * и равен нулю на минимальном, поэтому карта всегда возвращается в центр,
       * когда её отдаляют обратно.
       */
      panLimit: 9,
    },
  },

  lights: {
    keyPosition: [-9, 16, 9],
    keyIntensity: 3.0,
    fillPosition: [12, 6, -8],
    fillIntensity: 0.55,
    ambientIntensity: 0.58,
    shadow: {
      enabled: true,
      /** 'pcf' | 'vsm' | 'basic'. PCF мягкий и учитывает radius. */
      type: 'pcf',
      mapSize: 2048,
      radius: 4,
      opacity: 0.34,
    },
  },

  interaction: {
    /** Подъём велаята при наведении и при выборе (единицы сцены). */
    hoverLift: 0.32,
    activeLift: 0.95,
    /** Коэффициент сглаживания (0..1 за кадр при 60 fps). */
    damping: 0.14,
    /** Клик по пустому месту снимает выбор. */
    deselectOnBackdrop: true,
  },

  debug: {
    /**
     * Клик по карте печатает координаты точки в консоль и кладёт их в буфер
     * обмена — так подбираются `at: [долгота, широта]` для новых объектов.
     * Включено только в `npm run dev`, в прод-сборке молчит.
     */
    pickCoordinates: import.meta.env?.DEV ?? false,
  },

  renderer: {
    maxPixelRatio: 2,
    antialias: true,
  },
};
